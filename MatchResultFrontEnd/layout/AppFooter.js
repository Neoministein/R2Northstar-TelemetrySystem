import getConfig from 'next/config';
import React, { useContext } from 'react';
import { LayoutContext } from './context/layoutcontext';

const AppFooter = () => {
    return (
        <div className="layout-footer gap-2">
            
        </div>
    );
};

export default AppFooter;
