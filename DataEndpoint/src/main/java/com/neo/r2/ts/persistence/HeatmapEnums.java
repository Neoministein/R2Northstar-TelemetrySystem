package com.neo.r2.ts.persistence;

public final class HeatmapEnums {

    public enum Type {
        PLAYER_POSITION,
        CUSTOM
    }

    public enum ProcessState {
        CALCULATING,
        FINISHED,
        FAILED
    }
}
